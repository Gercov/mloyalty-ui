import type { FunctionalComponent, SVGAttributes } from "vue";
import checkmark from "./checkmark.svg?component";

export default { checkmark } as Record<
  string,
  FunctionalComponent<SVGAttributes, {}>
>;
